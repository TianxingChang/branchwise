import {
  attach,
  back,
  destroyUnder,
  forward,
  hide,
  navigate,
  open,
  place,
  reload,
} from "./handlers";

export const view = {
  attach,
  back,
  destroyUnder,
  forward,
  hide,
  navigate,
  open,
  place,
  reload,
};
